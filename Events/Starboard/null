		// if (!mediaData?.tweetData?.tweet.video_url && !mediaData?.videos) {
		// 	console.log("Twitter images")
		// 	//Make the lines pretty :)
		// 	const intData = `♥️ [${bot.toThousands(mediaData.tweetData.tweet.likes)}] 🔃 [${bot.toThousands(mediaData.tweetData.tweet.retweets)}]`;
		// 	const wrapLines = '─'.repeat(bot.MinMax(intData.length / 1.5, 1, 18));
		// 	for await (const photo of mediaData.tweetData.tweet.media_urls) {
		// 		const embed = new MessageEmbed()
		// 			.setColor('#c2b04e')
		// 			.setURL(message.url)
		// 			.setDescription(
		// 				`${mediaData.tweetData.tweet.description}\n${wrapLines}\n${intData}\n${wrapLines}\n\n${
		// 					message.content
		// 				}\n\n[Click to jump to message](${message.url})\nStarred› ${bot.relativeTimestamp(Date.now())}`
		// 			)
		// 			.setAuthor({ name: message.member.displayName, iconURL: message.member.displayAvatarURL({ dynamic: true }) })
		// 			.setImage(photo)
		// 			.setFooter({ text: `MessageID: ${message.id}` });
		// 		embeds.push(embed);
		// 	}
		// }
		// if (mediaData?.tweetData?.tweet?.video_url) {
		// 	console.log("Twitter video")
		// 	//Make the lines pretty :)
		// 	const intData = `♥️ [${bot.toThousands(mediaData.tweetData.tweet.likes)}] 🔃 [${bot.toThousands(mediaData.tweetData.tweet.retweets)}]`;
		// 	const wrapLines = '─'.repeat(bot.MinMax(intData.length / 1.5, 1, 18));

		// 	const embed = new MessageEmbed()
		// 		.setColor('#c2b04e')
		// 		.setURL(message.url)
		// 		.setDescription(
		// 			`${mediaData.tweetData.tweet.description}\n${wrapLines}\n${intData}\n${wrapLines}\n\n${
		// 				message.content
		// 			}\n\n[Click to jump to message](${message.url})\nStarred› ${bot.relativeTimestamp(Date.now())}`
		// 		)
		// 		.setAuthor({ name: message.member.displayName, iconURL: message.member.displayAvatarURL({ dynamic: true }) })
		// 		.setFooter({ text: `MessageID: ${message.id}` });
		// 	embeds.push(embed);

		// 	if (mediaData.tweetData.tweet.video_url) {
		// 		attachment = new MessageAttachment(mediaData.tweetData.tweet.video_url, `media.mp4`);
		// 	}
		// }

		// if (!mediaData.images && !mediaData.videos && !mediaData.tweetData) {
		// 	console.log("what are you")
		// 	const embed = new MessageEmbed()
		// 		.setColor('#c2b04e')
		// 		.setDescription(`${message.content}\n\n[Click to jump to message](${message.url})\nStarred› ${bot.relativeTimestamp(Date.now())}`)
		// 		.setAuthor({ name: message.member.displayName, iconURL: message.member.displayAvatarURL({ dynamic: true }) })
		// 		.setImage(finalImage)
		// 		.setFooter({ text: `MessageID: ${message.id}` });
		// 	embeds.push(embed);
		// }

		// if (mediaData.images) {
		// 	console.log("Images")
		// 	//Generate embed for images
		// 	for await (const media of mediaData.images) {
		// 		const embed = new MessageEmbed()
		// 			.setColor('#c2b04e')
		// 			.setURL(message.url)
		// 			.setDescription(`${message.content}\n\n[Click to jump to message](${message.url})\nStarred› ${bot.relativeTimestamp(Date.now())}`)
		// 			.setAuthor({ name: message.member.displayName, iconURL: message.member.displayAvatarURL({ dynamic: true }) })
		// 			.setImage(media)
		// 			.setFooter({ text: `MessageID: ${message.id}` });
		// 		embeds.push(embed);
		// 	}
		// }

		// if (mediaData?.videos) {
		// 	console.log("Videos")
		// 	//Video Embed
		// 	const embed = new MessageEmbed()
		// 		.setColor('#c2b04e')
		// 		.setURL(message.url)
		// 		.setDescription(`${message.content}\n\n[Click to jump to message](${message.url})\nStarred› ${bot.relativeTimestamp(Date.now())}`)
		// 		.setAuthor({ name: message.member.displayName, iconURL: message.member.displayAvatarURL({ dynamic: true }) })
		// 		.setFooter({ text: `MessageID: ${message.id}` });
		// 	embeds.push(embed);
		// 	if (mediaData.videos.length > 0) {
		// 		attachment = new MessageAttachment(mediaData.videos[0], `media.mp4`);
		// 	}
		// }